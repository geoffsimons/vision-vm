services:
  vision-vm:
    build: .
    environment:
      - DISPLAY=:99
    ports:
      - "5555:5555"
      - "5900:5900"
      - "9222:9222"
    volumes:
      - ./captures:/captures
      - ./chrome-data:/chrome-data
